'use client'

// Phase 2: fullscreen 3D hero text with drei <Text>, camera animation, zone navigation.
// Phase 1: stub — HTML hero section remains active overlay.
export function HeroZone() {
  return null
}
